# Archived message packs

`ar.json` — Arabic UI pack archived (not in the he/en/ru locale trio). Locale negotiation maps `ar` → `en` (`lib/i18n/config.ts`). Restore only with full key parity across the active packs.
