# תסריט קריינות ל-ElevenLabs — סרטון פרסום BSD-YBM OS

> הפקה: 6 שורות נפרדות, סרטון של 30 שניות (16:9 + 9:16).
> **חשוב: הפיקו כל שורה בנפרד והורידו 6 קבצים נפרדים** — כך אפשר למקם כל שורה בדיוק על הסצנה שלה.

---

## הגדרות מומלצות ב-ElevenLabs

| הגדרה | ערך |
|---|---|
| **Model** | `Eleven Multilingual v2` (או v3 אם זמין) — חובה, התומך בעברית |
| **Voice** | קול **גברי** בוגר וחם. מומלץ לנסות: `Adam`, `Antoni`, `Daniel`, `George` |
| **Stability** | ~50% (נמוך מדי = לא יציב, גבוה מדי = שטוח) |
| **Similarity** | ~75% |
| **Style Exaggeration** | 0–20% (נמוך — זה פרסומת עסקית, לא דרמה) |
| **Speaker Boost** | פעיל |

**טיפ:** אם מילה לועזית נשמעת לא נכון — כתבו אותה בתעתיק עברי (למשל `AI` ← `איי-איי`, `PO` ← `פי-או`).

---

## 6 השורות — להעתקה אחת-אחת

### שורה 1 — פתיחה
*(תקציב זמן: עד 3.6 שניות)*

```
מערכת אחת שמנהלת את כל העסק.
```

---

### שורה 2 — סריקה חכמה
*(תקציב זמן: עד 4.8 שניות)*

```
מצלמים מסמך, וה-AI קולט, מחלץ ומתייק אותו לבד.
```

---

### שורה 3 — ששת המרכזים
*(תקציב זמן: עד 5.0 שניות)*

```
פיננסים, פרויקטים, מסמכים ורכש. הכול מחובר.
```

---

### שורה 4 — הפיצ'רים הייחודיים
*(תקציב זמן: עד 4.8 שניות)*

```
מחולל אפליקציות בונה לכם כלים, וקופיילוט שטח מפיק הצעות.
```

---

### שורה 5 — סנכרון
*(תקציב זמן: עד 3.2 שניות)*

```
והכול מסונכרן בזמן אמת.
```

---

### שורה 6 — סיום וקריאה לפעולה
*(תקציב זמן: עד 4.0 שניות)*

```
מערכת אחת לכל העסק. היכנסו עכשיו.
```

---

## איך לשמור

הורידו את 6 הקבצים ושמרו אותם **בתיקייה אחת** (למשל `C:\Users\User\Downloads\vo\`)
עם השמות:

```
vo1.mp3
vo2.mp3
vo3.mp3
vo4.mp3
vo5.mp3
vo6.mp3
```

אם ElevenLabs נותן שמות אחרים — לא נורא, פשוט אמרו לי באיזו תיקייה הם ובאיזה סדר.

---

## מה קורה אחר כך

מסרו לי את נתיב התיקייה, ואני:
1. אמדוד את האורך המדויק של כל שורה
2. אתאים את תזמון הסצנות בסרטון כך שכל שורה תשב בול על הסצנה שלה
3. אמזג עם מוזיקת הרקע, אאזן עוצמות ואנרמל את הפסקול
4. אפיק מחדש את שתי הגרסאות (16:9 ו-9:16)

---

## גיבוי: אם רוצים לוותר על פיצול ל-6 קבצים

אפשר להפיק **קובץ אחד** עם כל הטקסט (עם שורה ריקה בין המשפטים כדי ליצור השהיות).
זה פחות מדויק בתזמון אבל עובד:

```
מערכת אחת שמנהלת את כל העסק.

מצלמים מסמך, וה-AI קולט, מחלץ ומתייק אותו לבד.

פיננסים, פרויקטים, מסמכים ורכש. הכול מחובר.

מחולל אפליקציות בונה לכם כלים, וקופיילוט שטח מפיק הצעות.

והכול מסונכרן בזמן אמת.

מערכת אחת לכל העסק. היכנסו עכשיו.
```
